/**
 * Custom React hooks directory.
 *
 * Place reusable hooks here as the app grows. Examples:
 *   - useJobs.ts      (jobs fetching with pagination)
 *   - useFilters.ts   (filter state management)
 *   - useSavedJobs.ts (saved job toggles)
 *
 * Import convention:
 *   import { useJobs } from '@/hooks';
 */

export {};
